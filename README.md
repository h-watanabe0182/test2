# ファイル検索ツール

指定したディレクトリ以下のファイルから文字列を検索するシンプルなコマンドラインツールです。

## 使い方

```
python search_tool.py DIRECTORY QUERY [--ignore-case]
```

- `DIRECTORY` ... 検索したいフォルダのパス
- `QUERY` ... 探したい文字列
- `--ignore-case`, `-i` ... 大文字小文字を区別しない

一致した行は `ファイルパス:行番号:行の内容` の形式で表示されます。


